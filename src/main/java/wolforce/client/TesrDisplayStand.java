package wolforce.client;

import net.minecraft.client.renderer.tileentity.TileEntitySpecialRenderer;
import net.minecraft.item.ItemStack;
import net.minecraft.util.EnumFacing;
import net.minecraftforge.items.CapabilityItemHandler;
import net.minecraftforge.items.IItemHandler;
import wolforce.UtilClient;
import wolforce.blocks.tile.TileSeparator;

public class TesrDisplayStand {// extends TileEntitySpecialRenderer<TileSeparator> { // TODO DIPLAY STAND

	// @Override
	// public void render(TileSeparator te, double x, double y, double z, float
	// partialTicks, int destroyStage, float alpha) {
	// IItemHandler itemh =
	// te.getCapability(CapabilityItemHandler.ITEM_HANDLER_CAPABILITY,
	// EnumFacing.NORTH);
	// ItemStack stack = itemh.getStackInSlot(0);
	// if (stack != null && UtilClient.canRenderTESR(te))
	// UtilClient.simpleRenderItem(te.getWorld(), stack, x, y, z, true);
	// }
}
